<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- sql 문을 적을 문서 -->
<mapper namespace="member">
	<select id="getList" resultType="memberDto">
		SELECT num, name, addr
		FROM member
		ORDER BY num ASC
	</select>
	<delete id="delete" parameterType="int">
		DELETE FROM member
		WHERE num=#{num}
	</delete>
	<insert id="insert" parameterType="memberDto">
		INSERT INTO member
		(num, name, addr)
		VALUES(member_seq.NEXTVAL, #{name}, #{addr})
	</insert>
	<select id="getData" parameterType="int" resultType="memberDto">
		SELECT num,name,addr
		FROM member
		WHERE num=#{num}
	</select>
	<update id="update" parameterType="memberDto">
		UPDATE member
		SET name=#{name}, addr=#{addr}
		WHERE num=#{num}
	</update>
	
	<!-- 안드로이드 테스트용 -->
	<resultMap type="java.util.HashMap" id="result">
		<result property="num" column="NUM"/>
		<result property="name" column="NAME"/>
		<result property="addr" column="ADDR"/>
	</resultMap>
	<select id="getList2" resultMap="result">
		SELECT num, name, addr <!-- select한 colum명이 대문자로 key에 들어간다. -->
		FROM member
		ORDER BY num ASC
	</select>
	<select id="memberGetList" resultMap="result">
		SELECT num, name <!-- select한 colum명이 대문자로 key에 들어간다. -->
		FROM member
		ORDER BY num ASC
	</select>
</mapper>